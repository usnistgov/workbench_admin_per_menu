<?php
class WorkbenchAdminPerMenuTest extends DrupalWebTestCase {

  public static function getInfo() {
    return array(
      'name' => 'workbench_admin_per_menu Unit Tests',
      'description' => 'I am the unit tests for workbench_admin_per_menu',
      'group' => 'Workbench'
    );

  }

  /**
   * I set up the class to be used for testing.
   */
  function setUp() {
    // inject dependencies
    parent::setUp('workbench_admin_per_menu');
  }

  public function testGeWAPMTaxonomyID() {
    $vid = geWAPMTaxonomyID();

    $result = false;
    if(is_numeric($vid)){
      $result = true;
    }

    $message = 'VID returned should be numeric.';
    $this->assertTrue($result, $message);
  }

  public function testGetWAPMUserHasDrupalAdminPermission(){
    //$loggedInUser

    $result = getWAPMUserHasDrupalAdminPermission();
    $message = 'Admin running tests should have admin rights.';
    $this->assertTrue($result, $message);

  }

}
